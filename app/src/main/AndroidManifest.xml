<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.emarketing.bicycle">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/login"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Fullscreen">
        <activity android:name=".ui.EventsActivity"></activity>
        <activity android:name=".ui.EditMaterial" />
        <activity android:name=".ui.ShopingActivity" />
        <activity android:name=".ui.MaterialDetails" />
        <activity android:name=".ui.AddMaterialActivity" />
        <activity android:name=".ui.Register" />
        <activity android:name=".ui.DoctorDetails" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.ProfileDetails" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".ui.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>